<Window x:Class="AgentRelay.Gui.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Agent Relay — Sign in"
        Width="440" Height="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0">
      <TextBlock Text="Agent Relay" FontSize="22" FontWeight="SemiBold"/>
      <TextBlock Text="Connect to your SolarNexus API server" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,16"/>
    </StackPanel>

    <StackPanel Grid.Row="1">
      <TextBlock Text="API base URL"/>
      <TextBox x:Name="UrlBox" Text="http://localhost:8080/api"/>
      <TextBlock Text="Email"/>
      <TextBox x:Name="EmailBox"/>
      <TextBlock Text="Password"/>
      <PasswordBox x:Name="PasswordBox"/>
    </StackPanel>

    <TextBlock x:Name="StatusText" Grid.Row="2" Foreground="#B91C1C" TextWrapping="Wrap" Margin="0,8,0,0"/>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button x:Name="QuitButton" Content="Quit" Click="QuitButton_Click"/>
      <Button x:Name="LoginButton" Content="Sign in" Click="LoginButton_Click" Background="{StaticResource AccentBrush}" Foreground="White"/>
    </StackPanel>
  </Grid>
</Window>
